import { DropDown } from "@/shared/buttons/dropdown"

export const Years = () => {
    return(
        <section className="flex items-center h-full flex-col   border-black ">
            <DropDown placeholder="Все годы"/>
        </section>
    )
}